---
title: Events API
description: POST /v1/events for audit event ingestion
---

# Events API

The Events API ingests audit records from the SDK. `ApiStorage` uses this endpoint when writing records.

## Endpoint

```
POST /v1/events
```

## Request

**Headers:**
- `Authorization: Bearer val_xxx`
- `Content-Type: application/json`

**Body:** Single `AuditRecord` or array of `AuditRecord[]` for batch ingestion.

```json
{
  "agent_id": "agent-weather-001",
  "team": "data-ops",
  "amount": "0.50",
  "amount_raw": "500000",
  "asset": "USDC",
  "network": "eip155:8453",
  "endpoint": "https://api.example.com/weather",
  "method": "GET",
  "tx_hash": "0xabc...",
  "payer_address": "0x...",
  "payee_address": "0x...",
  "policy_evaluation": "allowed",
  "created_at": 1700000000000,
  "tags": ["production"],
  "metadata": {}
}
```

## Response

**200 OK** — Event(s) accepted.

```json
{
  "accepted": 1,
  "ids": ["a1b2c3d4e5f6"]
}
```

**400 Bad Request** — Invalid payload.

**401 Unauthorized** — Invalid or missing API key.

## Batching

`ApiStorage` batches records and POSTs them according to `batchSize` and `flushIntervalMs`. You typically don't call this endpoint directly — use `ApiStorage` in the SDK.
